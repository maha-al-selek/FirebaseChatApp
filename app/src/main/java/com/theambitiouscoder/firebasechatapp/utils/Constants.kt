package com.theambitiouscoder.firebasechatapp.utils

class Constants {
    companion object{
        var TAG = "SimpleChatAppTag"
        const val BASE_URL = "https://fcm.googleapis.com"
        const val SERVER_KEY = "YOUR_SERVER_KEY" // get firebase server key from firebase project setting
        const val CONTENT_TYPE = "application/json"
        const val FIREBASE_INSTANCE_URL = "YOUR_URL" // get firebase instance url from firebase project setting
    }
}