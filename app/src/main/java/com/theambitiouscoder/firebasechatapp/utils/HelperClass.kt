package com.theambitiouscoder.firebasechatapp.utils

import android.util.Log
import com.theambitiouscoder.firebasechatapp.utils.Constants.Companion.TAG

object HelperClass {
    fun logErrorMessage(errorMessage: String) {
        Log.d(TAG, errorMessage)
    }
}